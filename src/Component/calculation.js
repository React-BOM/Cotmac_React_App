import React from "react";

const Calculation = ({ di, doValue, ai, aiHart, tc, rtd, ao, adh, mrtu }) => {
    let di1 = Number(di);
    let dO = Number(doValue);
    let ai1 = Number(ai);
    let aiHart1 = Number(aiHart);
    let tc1 = Number(tc);
    let rtd1 = Number(rtd);
    let ao1 = Number(ao);
    let adh1 = Number(adh);
    let mrtu1 = Number(mrtu);
  return (
    <div className="calculation-container">
    </div>
  );
};

export default Calculation;
